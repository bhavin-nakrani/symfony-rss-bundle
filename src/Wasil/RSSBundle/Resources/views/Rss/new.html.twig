{% extends 'WasilRSSBundle::layout.html.twig' %}

{% block body -%}
    <h1>Rss creation</h1>

    <form action="{{ path('rss_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('rss') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
